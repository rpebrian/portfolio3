<%- include("partials/header"); -%>
     <h1>Home</h1> 
    <p style="text-align: justify;"> <%= startingContent %> </p>

    <% Posts.forEach(function(post){ %>
         
            <h1><%= post.title %></h1>
            <p style="text-align: justify;"><%= post.content.substring(0, 100) + "....." %> <a href="/posts/<%= post.title %>">Read More</a></p>
            
            
    <% }) %>

    <%- include("partials/footer"); -%>

